---
title: "Edition 2027"
layout: default
permalink: "/editions/2027/"
---

<section class="mt-4 mb-5">
  <div class="container mb-4">
    <div class="d-flex align-items-center justify-content-between flex-wrap">
      <h1 class="font-weight-bold title mb-2">Edition 2027</h1>
      <a class="btn btn-outline-secondary mb-2" href="{{site.baseurl}}/editions/">All editions</a>
    </div>

    <p class="text-muted">Posts tagged with <code>edition: 2027</code>.</p>

    <div class="row">
      {% assign posts_in_year = site.posts | where: "edition", 2027 %}
      {% if posts_in_year and posts_in_year.size > 0 %}
        {% for post in posts_in_year %}
          {% include card-post.html %}
        {% endfor %}
      {% else %}
        <div class="col-12">
          <div class="alert alert-light border">
            No posts yet for 2027. Create one in <code>_posts/</code> with front matter:
            <pre class="mb-0"><code>---
title: "Your post title"
edition: 2027
layout: post
---
</code></pre>
          </div>
        </div>
      {% endif %}
    </div>
  </div>
</section>
